# Copy to .env in your working directory (gitignored). Never commit the real file.
IGNITION_URL=http://localhost:8088
IGNITION_API_TOKEN=keyname:secret-shown-once-when-the-api-key-is-created
IGNITION_DATA_DIR=/path/to/ignition/data
# IGNITION_PROJECT=Global
# IGNITION_TAG_PROVIDER=default
# IGNITION_STATE_DIR=.ign
